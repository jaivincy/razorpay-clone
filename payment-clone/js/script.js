document.addEventListener('DOMContentLoaded',()=>{const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');if(e.target.classList.contains('counter')){const target=+e.target.dataset.target,suffix=e.target.dataset.suffix,start=performance.now(),duration=1300;const tick=now=>{const v=Math.min((now-start)/duration,1)*target;e.target.textContent=(target%1?v.toFixed(1):Math.floor(v))+suffix;if(v<target)requestAnimationFrame(tick)};requestAnimationFrame(tick)}observer.unobserve(e.target)}}),{threshold:.15});document.querySelectorAll('.reveal,.counter').forEach(x=>observer.observe(x));const back=document.querySelector('.back-top');addEventListener('scroll',()=>back.classList.toggle('visible',scrollY>550));document.querySelectorAll('.faq-list details').forEach(d=>d.addEventListener('toggle',()=>{if(d.open)document.querySelectorAll('.faq-list details').forEach(o=>{if(o!==d)o.open=false})}));const words=['PayFlow.','clarity.','momentum.'];let wi=0,ci=0,deleting=false,el=document.getElementById('typing');function type(){let w=words[wi];el.textContent=w.slice(0,ci);if(!deleting&&ci++===w.length){deleting=true;setTimeout(type,1500);return}if(deleting&&ci--===0){deleting=false;wi=(wi+1)%words.length}setTimeout(type,deleting?55:100)}type()});
